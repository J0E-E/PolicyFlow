# P1.8 — Seed data, demo-session lifecycle & reset — Epic Plan

Source TDD: [./tdd-P1.8-seed-data-demo-session-lifecycle-reset.md](./tdd-P1.8-seed-data-demo-session-lifecycle-reset.md)

> **Review budget:** ~150 changed lines · ~8 non-generated files · one focused commit per epic. Tunable per project.

> High-level agile roadmap. Each epic's design specifics are confirmed with stakeholders at epic time (`4-plan-epic`) before any code is written.

This is an **L phase**. The order is simplest-first behind a tracer bullet: Epic 1 fires a thin
mint → carry → tag → observe thread end-to-end to prove the demo-session identity, then the
visible countdown lands, then write-tagging, read isolation, the masked-read markers, per-session
seed instantiation + the richer shared baseline, the purge engine + its triggers (scheduler, CLI,
session reset), graceful expiry, deploy-config alignment, and finally the acceptance gate. Slices
go vertical — every UI-bearing epic ships its backend and carries ` [UI]`. The `demo_session_id`
column + the always-`None` event-envelope field already exist as P1.7 seams; this phase fills them.

## Epic 1 — Tracer bullet: mint, carry, tag, observe — **COMPLETED** (50m · 34.8M tok · 694k tok/min)
- **Goal:** Prove the demo-session identity end-to-end — a visit mints a server-side session carried in its own cookie, and an agent-created lead plus its `lead.created` event both carry that session's id.
- **Rough scope:** The `platform.demo_sessions` table (migration) + a session lifecycle module (`ensure_demo_session` reuse-or-mint, `current_demo_session` read-only resolve) + the `pf_demo_session` cookie; `assume-persona` mints/reuses on each call; thread `demo_session_id` through `create_lead` and `build_envelope` so the agent-intake path tags the row and the event. Introduces the `DEMO_SESSION_LIFETIME_SECONDS` knob (default 86400) that sets `expires_at`.
- **Open questions / decisions for stakeholders:** none — resolved at plan time. (1) `pf_demo_session` cookie mirrors `pf_session` (HttpOnly, SameSite=Lax, Secure per `session_cookie_secure`, path=/), value = the **raw** session UUID, `max_age=DEMO_SESSION_LIFETIME_SECONDS` (86400) set **once at mint** and not refreshed on reuse (fixed 24h, no sliding). (2) Log one `INFO` line **on mint only** — `session id` + `expires_at`; reuse is silent (hot path).
- **Depends on:** none.
- **Implementation notes:**
  - **Plan deviation / cross-epic fact:** `0011` also grants each tenant role (+ `platform_reader`) `USAGE ON SCHEMA platform` + `SELECT ON platform.demo_sessions`, beyond the "table only" plan. The tag path runs under `SET LOCAL ROLE tenant_<x>` (via `get_tenant_db`), which otherwise can't read the `platform` schema — without the grant the tracer fails with "permission denied for schema platform". Epic 3 (public-intake tag, runs under `get_public_tenant_db`) and Epic 4 (scoped reads) inherit this grant; Epic 9 still owns the separate `demo_purge` DELETE role + per-tenant index.
  - **Cross-epic fact:** `current_demo_session(request, db)` takes `db` (not the bare `(request)` the TDD §5.1 sketched) — it must query `platform.demo_sessions`. Epic 2's `GET /api/demo/session` and Epic 6's masked-read marker must pass a session in. It is a plain async helper, not a FastAPI `Depends` dependency.

## Epic 2 — Session-state endpoint + live masthead countdown [UI] — **COMPLETED** (48m · 34.6M tok · 706k tok/min)
- **Goal:** A public read of the current demo session feeds a live `DEMO SESSION · HH:MM REMAINING` mono countdown on the workspace masthead, ticking locally from `expires_at` — replacing the static P1.6 stamp.
- **Rough scope:** Public `GET /api/demo/session` returning `{status, demo_session_id?, expires_at?, last_tenant_slug?}` (status ∈ active/expired/none) read from the cookie; the masthead countdown component consuming it. The endpoint is the shared seam later epics (graceful expiry, fresh session) reuse.
- **Open questions / decisions for stakeholders:** none — resolved at plan time.
- **Depends on:** Epic 1.
- **Implementation notes:**
  - **Decisions settled at plan time (stakeholder):** (a) format `DEMO SESSION · HH:MM REMAINING` — hours:minutes, tabular mono figures, minute digit ticks each minute (Guide §6.5); (b) `expired`/`none`/loading/error → plain "DEMO SESSION" overline stamp (no number), and a live tick reaching zero **freezes at 00:00** — the friendly "session ended" notice is **Epic 12**, not here; (c) **pure local tick** — fetch `GET /api/demo/session` once on mount, count down locally from `expires_at`, no polling; (d) explainer copy fix only — the countdown is now "real", not a "static placeholder".
  - **Cross-epic fact:** `GET /api/demo/session` + a new tri-state `read_demo_session_state(request, db)` resolver are the seam **Epic 12** reuses to tell "session ended" (`expired`) from a plain not-found (`none`); the `expired` branch returns `last_tenant_slug` so Epic 12's fresh-mint can preserve the tenant. Pure-local-tick means a server-side purge/reset mid-view (Epics 9/10/11) isn't reflected until reload — **Epic 12** owns catching that live.
  - **Plan deviation:** adds a *separate* `read_demo_session_state` rather than reusing `current_demo_session` (Epic 1's note suggested) — the latter collapses `expired`→`None`, so it cannot report the `expired` status this endpoint's contract requires.
  - **Cross-epic test gotcha (Epic 2):** `<Masthead>` now mounts the self-contained `<DemoSessionCountdown>`, which calls `getDemoSession()` on mount. Any test rendering the real signed-in shell (e.g. the `RequireSession` / `SelectTenantPage` happy paths) must add `getDemoSession` to its `vi.mock("../api")` factory — resolved to `{status:"none"}` for the plain-stamp steady state — or the unmocked call crashes the test after render.

## Epic 3 — Public intake auto-mint + tagging — **COMPLETED** (25m · 20.3M tok · 796k tok/min)
- **Goal:** A self-service lead submitted on the unauthenticated Shopper surface auto-mints a demo session (if none) and is tagged with its id — so visitor-created leads on both routes now carry the session.
- **Rough scope:** `POST /api/public/intake` calls `ensure_demo_session` (sets the cookie on the response) and passes the id into the shared `create_lead`; the claim/qualify/reject/resolve-duplicate actions also carry the session id onto their events.
- **Open questions / decisions for stakeholders:** none — resolved at plan time.
- **Depends on:** Epic 1.
- **Implementation notes:** none — shipped exactly as planned.

## Epic 4 — Read isolation: visibility predicate — **COMPLETED** (28m · 10.7M tok · 373k tok/min)
- **Goal:** One visitor never sees another's leads in the shared tenant schema — list, queue, and detail reads return only seed rows (`demo_session_id IS NULL`) plus the caller's own session rows; another session's row resolves to a 404, identical to the cross-tenant case.
- **Rough scope:** A small `visible_to_session` query helper applied to `list_leads` (+ queue filter) and, by post-load check, `get_lead`; `None` session ⇒ seed-only.
- **Open questions / decisions for stakeholders:** none — resolved at plan time. A foreign-session row resolves to `404 "lead not found"` (not 403), identical to the existing cross-tenant not-found (TDD D3).
- **Depends on:** Epic 1.
- **Implementation notes:**
  - **Cross-epic fact:** Epic 4 scopes only the **read** path (list/queue/detail). The same foreign-session `404` on the **action** endpoints (claim/qualify/reject/resolve-duplicate) is **Epic 5's** — it already opens every action endpoint for its seed-row `409` guard, so the foreign-session post-load `404` folds into that one write-isolation pass (settled at plan time; TDD §5.3 lists "every action endpoint").
  - **Cross-epic fact (Epic 4):** `core/app/leads/visibility.py::visible_to_session(query, demo_session_id)` is the one isolation predicate — Epic 5's matcher scoping (`find_duplicate_lead`) reuses it. `list_leads`/`get_lead` now take `request: Request` and resolve the session via `current_demo_session(request, db)`, so the read path already carries the `request` any future session work needs.

## Epic 5 — Matcher scoping + seed-row write guard — **COMPLETED** (41m · 40.7M tok · 971k tok/min)
- **Goal:** Duplicate detection respects session isolation (a visitor's submission only flags against seed ∪ their own rows), and mutating actions refuse to alter a shared seed row.
- **Rough scope:** Apply the visibility predicate inside `find_duplicate_lead`; add a defense-in-depth `409` when a claim/qualify/reject/resolve-duplicate targets a `demo_session_id IS NULL` seed row. The seeded dup-bait stays `NULL` so "Try a duplicate" still flags for everyone.
- **Open questions / decisions for stakeholders:** none — resolved at plan time. The foreign-session `404` pass **also covers `reveal`** (the PII-decrypting read the TDD §5.3 left unnamed): `404` only, no seed `409`. Defense-in-depth — foreign ids are never API-exposed, so near-unexploitable, but the isolation contract then holds without an asterisk.
- **Depends on:** Epic 4.
- **Implementation notes:**
  - **Scope deviation:** the foreign-session `404` also covers `POST /api/leads/{id}/reveal` (outside the TDD §5.3 enumeration) — `404` only, no seed `409` (revealing shared seed PII is fine).
  - **Plan refinement (load-bearing for Epics 6/11/14):** the seed-row `409` fires **only when the caller is in a live demo session**, not on every `demo_session_id IS NULL` row — `NULL` is also every ordinary non-demo lead, so a session-less caller acts on `NULL` rows normally (symmetric with the read path's `None`-session ⇒ seed-baseline). **Epic 6** (hide actions on `is_seed`) and **Epic 11** (session reset) must honor this gating, not the unconditional reading. Shared helper `_guard_loaded_lead_for_session(lead, request, db, *, refuse_seed)` (router.py) does both the 404 and the gated 409; `get_lead`/the 4 actions/`reveal` all route through it.
  - **Cross-epic fact (Epic 7):** `find_duplicate_lead` now takes `demo_session_id` and applies `visible_to_session`; `create_lead` threads it through. Epic 7's session-tagged dup-bait (vs. the shared `NULL` bait) relies on this — a visitor's "Try a duplicate" flags against seed ∪ their own only.

## Epic 6 — Masked-read session markers + list/detail UI [UI] — **COMPLETED** (39m · 32.2M tok · 817k tok/min)
- **Goal:** The UI distinguishes shared seed rows from "your session" rows — masked reads expose `is_seed` and `is_session_record` (never the raw id), lists/detail show a "YOUR SESSION" marginal tag, and mutating actions hide on seed rows.
- **Rough scope:** Add the two derived markers to `build_masked_lead` (given the caller's session id); render the tag + label shared seed/overlays + hide actions on `is_seed` in the leads list and detail views.
- **Open questions / decisions for stakeholders:** none — resolved at plan time. Both markers render as a neutral `StampTag` (no new component/hue): in the leads-list **name cell** (beside the duplicate stamp) and in the detail **header row** (beside the status stamp). `is_session_record` → **"YOUR SESSION"**; `is_seed` → **"SHARED SAMPLE"** with a `title` tooltip — read-only is conveyed by the absent action controls, not extra text. **No** extra row de-emphasis on seed rows (label + hidden actions only). Both markers are gated on a live demo session (Epic 5 fact), so a session-less caller sees neither tag and keeps full controls. Reveal stays available on seed rows (mutating actions only hide).
- **Depends on:** Epic 4.
- **Implementation notes:**
  - **Cross-epic fact (Epics 7/8/11/12/14):** `build_masked_lead` now takes `demo_session_id=None` and returns two derived booleans `is_seed` / `is_session_record` (never the raw id), gated like Epic 5's write guard so both are `False` for a session-less caller. Any epic returning a masked lead **must pass the caller's session id** or the markers silently read `False`.
  - **Cross-epic fact (Epics 7/8):** `is_seed` / `is_session_record` are **required** on `MaskedLead` (backend + TS type) — a future required field on this shape must update every literal lead fixture across the frontend tests, not just the `makeLead` helpers.
  - **Plan deviation:** `StampTag` gained an optional `title?: string` passthrough on the wrapper `<span>` (to attach the "SHARED SAMPLE" tooltip while reusing the one neutral component — no new component); default-absent, so existing stamps are unchanged.

## Epic 7 — Per-session seed instantiation + ledger — **COMPLETED** (50m · 33.0M tok · 649k tok/min)
- **Goal:** Each tenant-scoped persona gets a private, genuinely claimable New queue instantiated from canonical templates on first `assume-persona`, idempotently — so concurrent visitors each work their own queue without touching shared seed.
- **Rough scope:** A `platform.demo_session_tenant_seed` ledger (migration) as the per-(session, tenant) idempotency marker; `ensure_session_leads` inserts the canonical New queue templates + the dup-bait as session-tagged rows (reusing the encrypt/blind-index path into the tenant schema) when no ledger row exists; refactor the boot seed module to split shared-historical from per-session templates. Platform Admin (tenantless) skips it.
- **Open questions / decisions for stakeholders:** none — resolved at plan time. (1) Per-session template set = the existing **3 New-queue fillers + the Jordan Rivera dup-bait** per tenant (4 rows), now session-tagged — kept lean because Epic 8's shared-historical set carries the broader "render non-trivially" weight; the per-session set only needs to be a genuinely-claimable working queue. (2) The ledger gets its **own** new migration `0012_demo_session_tenant_seed` (the `0011` docstring already reserves it for this epic); Epic 9 then takes `0013` for the `demo_purge` role + `leads.demo_session_id` index.
- **Depends on:** Epic 4.
- **Implementation notes:**
  - **Cross-epic fact (Epic 9):** migration `0012` is consumed here (grants each tenant role `SELECT,INSERT` on the ledger for the in-transaction write) — so Epic 9's separate `demo_purge` `DELETE` role + per-tenant `leads.demo_session_id` index land in `0013`.
  - **Cross-epic fact (Epic 8) — `seed_demo_leads` is gone, not emptied.** The New-queue set moved out of the boot seed into per-session instantiation: `DEMO_LEADS` was renamed to `app.seed.SESSION_LEAD_TEMPLATES` (now consumed only by `ensure_session_leads`) and `seed_demo_leads` + its `seed()` call were **deleted** — the boot seed touches no `leads` table at all. Epic 8 must add the shared-historical set as a **new** boot-seed function; there is no `seed_demo_leads` to extend. `JORDAN_RIVERA_BAIT_EMAIL`/`_PHONE` stay in `seed.py` (frontend `shopperIntakePrefills.ts` mirrors them verbatim).
  - **Plan deviation:** `ensure_session_leads` takes the `TenantConfig` (not a bare slug) and resolves the owning `tenant_id` from `platform.tenants` on the login-role `db` **before** the scoped block — the scoped tenant role can read only `demo_sessions` + the ledger, not `platform.tenants`. `assume_persona` calls it only when `persona_identity.tenant_id is not None` (tenantless Platform Admin skips). Idempotency is the **ledger only** — the boot seed's `email_blind_index` skip is deliberately not used (a second visit shares the bait email yet must seed its own copy).

## Epic 8 — Shared read-only historical seed expansion — **COMPLETED** (45m · 25.4M tok · 556k tok/min)
- **Goal:** Lists and dashboards render non-trivially from seed alone — the shared `NULL` baseline gains a richer set of worked/historical leads (Qualified / Rejected / owned) per tenant that give context without being claimable.
- **Rough scope:** Expand the shared-historical side of the boot seed (the side Epic 7 split out) with a curated cross-status lead set per tenant; these stay `NULL` (read-only, visible to all).
- **Open questions / decisions for stakeholders:** none — resolved at plan time. **6 historical leads per tenant: 2 Working / 2 Qualified / 2 Rejected.** All 6 **owned, split 3/3 across `agent.one` + `agent.two`**; Rejected = `Working → Rejected`, each carrying a `rejection_reason` (no unowned/`New` rows — those would surface in the Unassigned-queue tab looking claimable but silently guarded). `created_at` **backdated** (spread over the last few weeks, computed relative to boot so it stays recent) so they sort below today's fresh session queue and read as history. All rows `demo_session_id IS NULL`; per-tenant idempotency is **count-based on `NULL`-session rows** (skip if any exist), mirroring `seed_pii_demo_records`. New boot-seed function `seed_shared_historical_leads`, called from `seed()` after the data-keys commit. No migration (the `leads` table + `demo_session_id` column already exist).
- **Depends on:** Epic 7.
- **Implementation notes:**
  - **Cross-epic fact (Epics 9/14):** the shared historical set is `demo_session_id IS NULL` — the purge engine (Epic 9) must never delete it (it scopes to `NOT NULL`); it **is** the "`NULL` baseline intact" Epic 14's acceptance asserts survives all three purge scopes.
  - **Cross-epic handoff (Epics 9/14):** the boot seed now leaves 6 `NULL`-session rows per tenant — any DB test doing an **unscoped** `COUNT(*)`/`SELECT … FROM <schema>.leads` after `seed()` and asserting an exact total must scope to `demo_session_id IS NOT NULL` (instantiated set) or `IS NULL` (historical baseline). Epic 7's `test_session_lead_instantiation.py` counters were re-scoped to `IS NOT NULL` for exactly this. Historical rows are `lead_source=agent_entered` (agent-worked records, not public submissions) — a later assertion on the shared set should expect that source.

## Epic 9 — Purge engine + `demo_purge` role + operator CLI — **COMPLETED** (53m · 20.7M tok · 388k tok/min)
- **Goal:** One engine purges a session's leads (and ledger rows) across every tenant schema, parameterized by scope (Expired / All / Session), runnable by hand — the foundation every reset trigger reuses.
- **Rough scope:** `purge_sessions(scope, *, delete_session_row)` opening its own dedicated-role session, deleting session-tagged `leads` across all registry tenant schemas then the ledger then (when asked) the `demo_sessions` rows, returning counts; the `demo_purge` DB role + cross-schema grants + a per-tenant `demo_session_id` index (migration); and `python -m app.demo.reset [--all|--expired]` reusing the engine (not reachable through the demo role switcher). Unit-proven across both schemas.
- **Open questions / decisions for stakeholders:** none — resolved at plan time. (1) **Observability** — one summary **INFO** line per purge run in the house parameterized-`logger.info` idiom (no new logging framework): `scope`, in-scope **session count**, per-tenant `leads_deleted` map, `ledger_deleted`, and `session_rows_deleted`; the in-scope **session ids** log at **DEBUG** only (an `All`/nightly run can name dozens). An **empty/no-op** run still logs at INFO (unlike the ~1s relay's suppressed empty sweep — the purge cadence is slow, so "ran, deleted nothing" is a wanted signal). (2) **Core-only scope confirmed** — purge deletes `leads` (both tenant schemas) + the `demo_session_tenant_seed` ledger + the `demo_sessions` rows only; `outbox`/`processed_events` and future sidecar stores are **not** purged (PII-free, self-expire) — the event `demo_session_id` tag readies the **M3** sidecar cascade (TDD Risk #5 / D5).
- **Depends on:** Epic 7.
- **Implementation notes:**
  - **The engine (Epics 10/11/14):** `app.demo.purge.purge_sessions(scope, *, delete_session_row)` is the one seam; scopes are frozen dataclasses `Session(id)` / `Expired()` / `All()` (union `PurgeScope`), returning `PurgeCounts` (`session_ids`, per-schema `leads_deleted` map, `total_leads_deleted`, `ledger_deleted`, `session_rows_deleted`). It holds a module-global `session_factory` (own-session, `demo_purge` role, one `SET LOCAL ROLE`, one transaction across **all** schemas — the relay/audit idiom). **Epic 10's scheduler** calls `Expired()/All()` with `delete_session_row=True`; **Epic 11's `POST /api/demo/session/reset`** calls `Session(cookie_id), delete_session_row=False` (keeps the row, cookie, expiry). Tests point the global at the container via a `container_purge_session_factory` fixture (mirrors `container_relay_session_factory`).
  - **Migration is `0013`** (`0011` table split + `0012` ledger were already taken): `demo_purge` role + grants + per-tenant `ix_leads_demo_session_id` index. `demo_purge` is the **only** role with DELETE on `leads`; the index is hand-owned (the `leads` ORM twin is excluded from `alembic check`, the `0009` precedent).
  - **Test-substrate carry-forward:** purge DB tests must `DELETE FROM platform.demo_sessions` in `_reset_state` (the shared container accumulates rows across files, so an `All()` scope would sweep strays and break exact-count assertions). CLI tests `await run(scope)` directly — `main`'s `asyncio.run` cannot nest inside the pytest-asyncio loop.

## Epic 10 — In-process purge scheduler — **COMPLETED** (47m · 14.3M tok · 301k tok/min)
- **Goal:** Expired sessions self-clean and the demo resets to canonical state nightly without manual action — a background task runs frequent expiry purges and a once-a-night scope=All at a quiet hour.
- **Rough scope:** A `demo_lifecycle` lifespan task (mirroring the event-bus lifespan) looping every `DEMO_PURGE_INTERVAL_SECONDS` running `purge(Expired)` and firing `purge(All)` once per crossing of `DEMO_NIGHTLY_RESET_HOUR_UTC`; wired into the app's lifespan stack. Introduces those two knobs.
- **Open questions / decisions for stakeholders:** none — resolved at plan time. (1) **No boot catch-up** — the nightly-reset last-run date is seeded to *today* at task start, so a boot after the reset hour does **not** fire `purge(All)`; the full wipe only ever fires at the predictable quiet-hour crossing (never on a deploy/restart). (2) **Defaults** `DEMO_PURGE_INTERVAL_SECONDS=300`, `DEMO_NIGHTLY_RESET_HOUR_UTC=4`, both env-overridable. (3) **Sweep-first** — `purge(Expired)` runs immediately on boot then sleeps the interval; each iteration wraps the purge in `try/except` → log & continue (mirrors `run_relay_loop`), single-replica assumed.
- **Depends on:** Epic 9.
- **Implementation notes:**
  - **Cross-epic fact (Epic 13):** this epic introduces the two `DEMO_*` knobs (`DEMO_PURGE_INTERVAL_SECONDS`, `DEMO_NIGHTLY_RESET_HOUR_UTC`) with defaults in `app.config`; Epic 13's deploy-config alignment wires/sets them in the demo env alongside the other `DEMO_*` knobs.
  - **Cross-epic fact (future lifespan tasks):** `main.py` is now a **composed** lifespan stack (`AsyncExitStack` entering `event_bus_lifespan` + `demo_lifecycle_lifespan`), not a single bare lifespan — any future startup/shutdown task adds another `async with` to that stack rather than replacing the `FastAPI(lifespan=…)` arg. The composed CM is `app.main.app_lifespan`; faked-composition tests monkeypatch `main.event_bus_lifespan` / `main.demo_lifecycle_lifespan` (the names `app_lifespan` resolves at call time) to recording CMs — no broker/DB needed.
  - **Test-substrate gotcha (carry-forward):** a loop unit test that fakes `asyncio.sleep` to a no-delay yield must capture the **real** `asyncio.sleep` *before* patching and call that captured ref inside the replacement — patching `runtime.asyncio.sleep` to a body that calls `asyncio.sleep(0)` mutates the shared module and recurses forever (`RecursionError`). See `tests/test_demo_lifecycle.py::_patch_no_delay_sleep`.

## Epic 11 — Session-scoped reset + workspace reset control [UI] — **COMPLETED** (50m · 27.5M tok · 549k tok/min)
- **Goal:** A visitor's role-switched Platform Admin can wipe their own session's data to a clean slate from the workspace — without touching any other visitor and keeping their session/cookie/countdown alive.
- **Rough scope:** `POST /api/demo/session/reset` (`require_platform_admin`) running `purge(Session(cookie id), delete_session_row=False)` — keeps the row + cookie + expiry, defers re-seed to the next `assume-persona` (ledger now cleared); a demo-controls reset affordance in the workspace calling it.
- **Open questions / decisions for stakeholders:** none — resolved at plan time. (1) **Placement** — the reset control lives in the **masthead right cluster**, beside the `DEMO SESSION` countdown stamp, rendered **only** for Platform Admin (globally reachable, semantically next to the session UI it resets). (2) **Confirm** — a **modal confirm dialog** (the Guide's Dialog pattern — scrim + focus trap, the DLQ-discard analog), reusing the existing focus-trap modal idiom (`ScenarioReferencePanel` + `useFocusTrap`); copy states what's deleted and that the session/countdown stays alive. (3) **Feedback** — **self-contained dialog** (the `RoleSwitcher` idiom): Reset button shows pending during the call; success shows a brief in-dialog confirmation with a *switch to Agent to see your fresh queue* nudge, then closes; failure shows an inline error and stays open to retry.
- **Depends on:** Epic 9.
- **Implementation notes:**
  - **Cross-epic fact (Epic 12):** reset resolves the live session via `current_demo_session(request, db)` (expired→`None`), returning `409 "no active demo session"` on `None` — not the raw cookie. Epic 12's graceful-expiry shares the `pf_demo_session` cookie and must keep relying on that same expired→`None` collapse. The pure-local masthead countdown is **not** refetched on reset, so the figure is unchanged until reload — **Epic 12** owns catching server-side purges live.
  - **Plan deviation (UI tokens):** the dialog sheet uses `--surface-2`, not the plan's `--surface-4` (the Epic 6 token scale stops at `--surface-3`; matches the existing `scenario-reference.css` dialog). The destructive Reset is a `filled` `Button` recolored to `--state-error` — the Guide has no `destructive` variant.

## Epic 12 — Graceful expiry [UI] — **COMPLETED** (2h 3m · 22.2M tok · 180k tok/min)
- **Goal:** An expired or unknown session never shows a raw 404/500 — the visitor sees a friendly "your previous demo session ended — demo data resets every 24 hours" notice with one click to a fresh session that preserves their tenant.
- **Rough scope:** Public `POST /api/demo/session` (`ensure_demo_session` with the remembered tenant) as the fresh-mint button; a `DemoSessionGate`/notice rendered when state is non-active or a deep-link lead read 404s while state is non-active (cross-checking the Epic 2 endpoint to tell "session ended" from a plain not-found); stepper progress resets with the session.
- **Open questions / decisions for stakeholders:** none — resolved at plan time. (1) **Notice = calm info Banner** (Guide §6.5/§4): a strip with a left info icon, the message, and a "Start a fresh session" action; informational tones (neutral/`--surface`), never `--state-error`. Copy: _"Your demo session ended. Demo data resets every 24 hours to keep the sandbox clean — your agency is preserved."_ (2) **Gate scope narrowed to the deep-link lead-detail `404` only** (not shell-wide) — the one surface that throws a raw not-found today; every other `/app` surface already fails soft (empty states, plain countdown stamp). (3) **The fresh-session button re-assumes the current persona** (the existing `assume-persona`), which mints a fresh demo session **and** re-seeds the queue in one call — rather than the planned public `POST /api/demo/session`. The gate only ever shows **inside `/app`**, where the visitor is always still authenticated, so the remembered-tenant public-mint endpoint isn't reachable this slice. (4) **Stepper-progress reset stays in slice, for free:** the button hard-reloads to `/app/leads` after re-assume — the in-memory guided docket re-initializes to step 1 on reload, and the masthead countdown re-fetches the fresh session (closing the live-refresh gap Epic 11 handed here).
- **Depends on:** Epic 2, Epic 4.
- **Implementation notes:**
  - **Plan deviation (scope narrowed at plan time) — frontend-only epic, no new backend.** The fresh-session button reuses `assume-persona` (re-mint + re-seed) instead of building the planned public `POST /api/demo/session`, and the gate is scoped to the deep-link lead-detail `404` only (not shell-wide).
  - **Cross-epic fact (Epic 14):** Epic 12 adds **no** new endpoint — Epic 14's acceptance must **not** assert a public `POST /api/demo/session`. The deferred public fresh-mint endpoint + the shell-wide / unauthenticated graceful-expiry gate are a backlog follow-up (`Deferred_Features_Backlog.md` #2).
  - **Cross-epic gotcha (Epic 2 realized) — `LeadDetailPage.test.tsx` now calls `getDemoSession` on every 404.** Its `vi.mock("../api")` factory gained `getDemoSession: vi.fn()`, reset + defaulted to `{ status: "active" }` in `beforeEach` so the pre-existing plain-404 test still reads as a genuine missing lead. Any future test that exercises a lead 404 must keep that default (or override per status). `window.location.assign` is stubbed (jsdom has no navigation) and restored per test.

## Epic 13 — Demo-deployment config alignment — **COMPLETED** (39m · 13.3M tok · 337k tok/min)
- **Goal:** The demo deployment runs the lifecycle correctly out of the box — the `DEMO_*` knobs are set, the auth session is aligned to 24h so no surprise re-login lands inside the demo window, and the purge role's wiring is confirmed alongside the other privileged roles.
- **Rough scope:** Build the missing env-plumbing seam (the core container reads only `DATABASE_URL`/`RABBITMQ_URL` today — add the config knobs to `core.environment` via `${VAR:-default}` substitution so `prod.env.defaults` values actually reach the app), then set the demo values: `SESSION_LIFETIME_SECONDS=86400` (8h stays the non-demo default), the three `DEMO_*` knobs explicitly, and `SESSION_COOKIE_SECURE=true`. Confirm/document that `demo_purge` needs no creds (NOLOGIN role granted by migration `0013`, run on deploy).
- **Open questions / decisions for stakeholders:** none — resolved at plan time. (1) **Env-plumbing seam = explicit `environment:` entries** under `core` in `docker-compose.yml` (`VAR: ${VAR:-default}`), mirroring the `DATABASE_URL` pattern — self-documenting, only named vars reach the container, local dev unchanged via the default fallback (not `env_file:`, which would inject every `.env` key). (2) **Scope = Tier 1 + `SESSION_COOKIE_SECURE`:** set `SESSION_LIFETIME_SECONDS=86400` + the three `DEMO_*` knobs (at their code defaults, set explicitly for out-of-the-box clarity) + `SESSION_COOKIE_SECURE=true` (cheap non-secret line; the demo runs behind TLS); **defer** the secrets `PII_MASTER_KEY`/`SEED_USER_PASSWORD` (real secrets needing new SSM params + an `after_install.sh` fetch + a security review) to a backlog hardening item — the seam built here makes folding them in later trivial. (3) **`demo_purge` needs no creds** — like `outbox_relay`/`audit_writer` it is a NOLOGIN role `GRANT`ed to the single login role by a migration (`0013`, shipped Epic 9), and migrations run on every deploy via core's entrypoint; there are no per-role passwords in SSM/Terraform for any privileged role, so the "wire creds" item is a confirm/document, not new infra. (4) **D9 confirmed** — 24h auth session (`SESSION_LIFETIME_SECONDS=86400`) for the demo env only removes the surprise re-login inside the 24h window; the realistic 8h stays the non-demo code default.
- **Depends on:** Epic 10.
- **Implementation notes:**
  - **Plan-premise correction (load-bearing for Epic 14):** the core container had **no env-plumbing seam** before this epic — it received only `DATABASE_URL`/`RABBITMQ_URL` (compose `${VAR}` *substitution*), so every other `config.py` knob silently used its code default in the deployment regardless of `prod.env.defaults`. This epic adds explicit `core.environment` entries so the deploy-time `.env` values actually reach the app; that seam is the real deliverable, not "set a value."
  - **Plan-premise correction:** "wire `demo_purge` creds through SSM/Terraform" is a no-op — `demo_purge` (and `outbox_relay`/`audit_writer`) are NOLOGIN roles granted to the login role by migrations (`0013`), reached via `SET LOCAL ROLE`; no privileged role has SSM/Terraform creds. Confirmed + documented only; no infra added.
  - **Deferred (backlog #3):** `PII_MASTER_KEY`/`SEED_USER_PASSWORD` are still on code/throwaway defaults in the deployment despite `config.py`'s "prod injects via SSM" comments. The seam built here is the prerequisite; the SSM-param + deploy-hook + security review is its own item.

## Epic 14 — Acceptance suite — **COMPLETED** (48m · 17.3M tok · 357k tok/min)
- **Goal:** A named acceptance suite proves the phase contract on the real substrate — cross-session rows hidden, created lead + its event carry the session id, expiry/all/session purges delete exactly their scope across both schemas leaving the `NULL` baseline intact, instantiation is idempotent via the ledger, and `GET /api/demo/session` reports active/expired/none correctly.
- **Rough scope:** `test_demo_session_acceptance.py` (real Postgres + RabbitMQ), test-only — **no production code**. One chained end-to-end narrative against the never-reset testcontainers (the `test_event_bus_acceptance.py` idiom): 5 append-only phases — (1) write-tagging proven through the broker (lead row + `lead.created` outbox row + the drained AMQP message's round-tripped envelope all carry `demo_session_id`); (2) `GET /api/demo/session` → active/expired/none; (3) ledger idempotency (a 2nd same-session `assume-persona` inserts no new leads); (4) read isolation (cross-session `GET /api/leads/{id}` → 404 + list exclusion, shared `NULL` baseline visible to both); (5) the three purge scopes across both tenant schemas (Session via `POST /api/demo/session/reset`, Expired+All via `purge_sessions(...)` direct), `NULL` baseline intact.
- **Open questions / decisions for stakeholders:** none — resolved at plan time. (1) **Hybrid drive style** — black-box through every public endpoint that exists (assume-persona, intake, leads, reset endpoint, session-state endpoint); call `purge_sessions(...)` direct **only** for Expired/All (no operator-facing HTTP trigger exists beyond the CLI). Row read-backs via the SELECT-capable superuser engine, schema-qualified. (2) **Event proof goes through the broker** — not the outbox row alone: publish via the relay, drain the AMQP message, round-trip the envelope, assert `demo_session_id` on the wire (the only proof needing real RabbitMQ; de-risks the M3 sidecar tag). (3) **Complementary E2E narrative** — deliberately overlaps the unit assertions but exercises them *through assembly*; every existing unit test (`test_demo_purge_scopes`, `test_lead_reads`, `test_demo_session`, `test_session_lead_instantiation`) stays untouched.
- **Depends on:** Epics 1–12.
- **Implementation notes:**
  - **Advanced (size-budget exception):** one file at ~300–450 lines, over the ~150-line budget — a single **atomic, test-only** acceptance artifact (no production code; bulk is mechanical entity-pinned read-back helpers, boilerplate the budget discounts). One file keeps the "single named proof" the TDD §8 asks for intact; splitting would fracture the narrative chain and duplicate the fixture/helper preamble. Human-approved at plan time.
  - **Substrate gotchas (durable, in-test only — no behavior change):** (1) `ensure_session_leads` opens `get_public_tenant_db`, which **rolls back any uncommitted transaction** before scoping to the tenant role — so a `demo_sessions` row must be committed *before* instantiating its overlay (the `_mint_demo_session` → `_seed_session_overlay` order). (2) There is no queue literally named `lead.created`; it is a routing key — the broker round-trip drains off the `sync.logger` consumer queue (binds `#`).
