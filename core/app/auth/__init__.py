"""The `auth` package: password hashing, sessions, RBAC, and the auth router.

This is the first module of the package (Epic 3 adds password hashing); later
epics fill in the auth provider, session store, role checks, dependencies, and
router.
"""
