# Test-only dependencies. Kept separate from requirements.txt so the runtime
# Dockerfile and production image stay free of test tooling.
pytest==8.3.4
httpx==0.28.1
pytest-asyncio==0.25.2
# Boots a throwaway Postgres in Docker for the real-database test substrate
# (Epic 11). The [postgres] extra pulls the docker SDK + the PostgresContainer.
testcontainers[postgres]==4.14.2
